package nadFinal;

// A server manages many clients
// Each client has a thread that communicates with the server
//  has a thread that listens for new clients
// The server has a thread that listens for messages from clients
public class GroupChatServer {

    // a modifiable list of ChatGroup objects with the name and port number of the
    // server
    // this list has a default chat group with the name "default" and port number
    // 6666

    // ArrayList<ChatGroup> chatGroups = new ArrayList<ChatGroup>();
    // create a default chat group with the name "default" and port number 6666
    // chatGroups.add(new ChatGroup("default", 6666));

    // this is the start method for the server
    public void start() {





        // // try block to handle exceptions
        // try {
        //     ClientThread clientThread = new ClientThread(6666);

        //     // start the client thread to handle the clients connection
        //     clientThread.start();
        // } catch (Exception e) {
        //     // display the exception on the console
        //     System.out.println(e);
        // }

    }

}
